# Vault — Secrets Manager

Личный менеджер секретов на HashiCorp Vault с автоматической распечаткой и резервным копированием.

## ⚠️ Важно

- **Секреты НИКОГДА не коммитятся в репозиторий**
- Unseal-ключи и `keys.enc` хранятся только локально
- Для доступа извне используй обратный прокси с аутентификацией

## 🚀 Быстрый старт

```bash
# Клонируем репозиторий
git clone https://github.com/ваш-юзернейм/Vault.git
cd Vault/compose

# Запускаем Vault
docker compose -f vault.yml up -d

# Инициализируем (ТОЛЬКО ПЕРВЫЙ РАЗ!)
docker exec -it vault vault operator init > ~/vault-keys.txt
# ⚠️ СОХРАНИТЕ ~/vault-keys.txt В НАДЁЖНОЕ МЕСТО!

# Распечатываем (3 раза)
docker exec -it vault vault operator unseal